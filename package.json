{
  "name": "homepage",
  "version": "1.0.0",
  "description": "My personal homepage",
  "author": "Markus Appel",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "build:full": "npm run build && npm run build:games",
    "build:games": "npm run build:breakout && npm run build:evolife",
    "build:breakout": "cd games/breakout && npm run build",
    "build:evolife": "cd games/EvolutronicLife && npm run build",
    "install:games": "npm run install:breakout && npm run install:evolife",
    "install:breakout": "cd games/breakout && npm install",
    "install:evolife": "cd games/EvolutronicLife && npm install",
    "setup:games": "git submodule update --init && npm run install:games && npm run build:games",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .eslintignore .",
    "lint:fix": "eslint --ext .js,.vue --ignore-path .eslintignore --fix ."
  },
  "lint-staged": {
    "*.{js,vue}": "npm run lint:fix"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.2",
    "amplitudejs": "^5.2.0",
    "cross-env": "^7.0.2",
    "express": "^4.16.4",
    "lodash-es": "^4.17.15",
    "minireset.css": "0.0.6",
    "nuxt": "^2.14.7"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^5.0.0",
    "@nuxtjs/eslint-module": "^3.0.1",
    "@nuxtjs/style-resources": "^1.0.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^7.13.0",
    "eslint-config-prettier": "^6.15.0",
    "eslint-plugin-nuxt": "^2.0.0",
    "eslint-plugin-prettier": "^3.1.4",
    "husky": "^4.3.0",
    "lint-staged": "^10.5.1",
    "node-sass": "^5.0.0",
    "nodemon": "^2.0.6",
    "prettier": "^2.1.2",
    "sass-loader": "^10.1.0"
  }
}
